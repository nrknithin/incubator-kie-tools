import { generateDocument } from "../src/index"; // Adjust path as necessary

describe("dmn-documentation-generator", () => {
  it("should have a placeholder test", () => {
    expect(true).toBe(true);
  });

  // Placeholder for generateDocument tests
  // describe("generateDocument", () => {
  //   it("should generate for browser", async () => {
  //     // const result = await generateDocument(...);
  //     // expect(result).toBeInstanceOf(Blob);
  //   });
  //
  //   it("should generate for node", async () => {
  //     // const result = await generateDocument(...);
  //     // expect(result).toBeInstanceOf(Uint8Array);
  //   });
  // });
});
